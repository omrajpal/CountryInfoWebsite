import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: '#6B705C',
    secondary: '#FFC947',
    background: '#FFB085',
    text: '#1D3557',
  },
});

export default theme;
